# Research package mechanical validation

2026-09-10, `/root`. All listed checks pass in the assembled package. These are
integrity checks, not semantic proof of every source claim; the independent audit
and later exact-entry source/tone reviews are separate.

- Source identity:257 physical pages,7,036,292 bytes and SHA-256
  `d4d66faa1169ae6c3623a4adaa23f611cb94c67625932b5dc53db48ffe91864b` match the
  original file and finalized manifest. All 257 cached page texts match fresh
  original extraction after whitespace normalization.
- JSON/JSONL parses; stable IDs are unique. All 174 source units,692 ledger rows,
  534 evidence records and 106 read logs resolve their references. The 257-row page
  register has no missing source unit, named read log or visual coverage.
- Every ledger row has one of the four required dispositions and a concrete reason.
  Included/combined ideas resolve evidence. Targets resolve stable idea/evidence,
  charter-principle IDs or care candidate aliases declared in evidence and indexed
  in `CANDIDATE_REGISTRY.jsonl`.
- All direct source references contain physical spans in 1-257, section/locator,
  source identity, printed-verification state, actual context and read-log links.
  All optional printed references are null: no unverified print citations.
- Every evidence record has the required contract fields. All 100 held records
  have explicit open questions; the status is never substituted by included or
  combined. Topics and shared-principle keys resolve to the finalized foundation.
- Source/section files remain under `research/toddler/`; scratch remains ignored
  under `tmp/toddler-research/`. No raw PDF text or copied artwork entered public
  assets. No app build is warranted for these research-only changes.

Counts by disposition:347 included,219 combined,77 context-only,49 excluded.
Evidence state:434 research-ready,100 held. Some holds preserve excluded older-age
material; they are not all in-scope candidate questions. Counts describe the work
and do not set population targets.

`VALIDATION.json`, `PART_COUNTS.json`, `SOURCE_IDENTITY.json` and
`EXTRACTION_CHECK.json` retain machine-readable results. Author-owned NOTES and
validation files record the section-level source/row checks. The initial root
validator was corrected to recognize declared candidate aliases and reduced nested
context locators; those were validator assumptions, not missing source evidence.
One temporary cross-part sharing target was corrected by its author to the
existing `toddler.care.ev.sharing-turns` before the frozen package.

After the independent audit, the authors corrected the PDF 245 paint-container
wording and retained the PDF 138 anticipatory tantrum list. The root package was
reassembled from those revised author records and validated again with no errors.
The separate audit records source rechecks and the final reviewed package hashes.
